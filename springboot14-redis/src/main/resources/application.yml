server:
  port: 8080
spring:
  #redis????
  redis:
    database: 5
    # ??redis??????????????
    host: 192.168.146.1
    port: 6379
    timeout: 5000
    jedis:
      pool:
        # ?????????????????8?
        max-idle: 500
        # ?????????????????0?
        min-idle: 50
        # ?????-1??????????pool?????maxActive?jedis??????pool????exhausted(??)
        max-active: 1000
        # ?????????????????????-1??????????????????????JedisConnectionException
        max-wait: 2000
